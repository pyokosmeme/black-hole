# Cloudflare Pages Functions configuration
# Pages Functions live in functions/ directory
# KV namespace binding: SESSIONS (configured in dashboard)
# No wrangler.toml needed for Pages Functions — dashboard handles bindings
